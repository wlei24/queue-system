spring:
  application:
    name: QUEUE-DATASYNC.SERVICE
server:
  port: 10080
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

